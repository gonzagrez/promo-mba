[build]
  command = "npm run build"
  publish = "dist"

# Para que la app (de una sola página) funcione al refrescar o al volver
# desde el enlace de recuperación de clave.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
